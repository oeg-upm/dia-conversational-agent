version: "3.9"

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: basicragchromadb
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/data
    restart: unless-stopped

volumes:
  chroma_data: