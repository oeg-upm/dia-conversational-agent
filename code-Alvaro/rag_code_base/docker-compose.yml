version: "3.9"

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: basicragchromadb_2
    ports:
      - "8002:8000"
    volumes:
      - chroma_data:/data
    restart: unless-stopped

volumes:
  chroma_data: